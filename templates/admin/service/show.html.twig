{% extends 'admin//base.html.twig' %}

{% block title %}ServiceProvider{% endblock %}

{% block body %}
    <h1>ServiceProvider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ service_provider.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ service_provider.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_index') }}">back to list</a>

    <a href="{{ path('app_service_edit', {'id': service_provider.id}) }}">edit</a>

    {{ include('admin/service/_delete_form.html.twig') }}
{% endblock %}
